apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev03-app-base
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/grantking/argocd.git
    targetRevision: dev-01
    path: apps
    helm:
      parameters:
        - name: "pm.namespace"
          value: "dev03-postman"

  destination:
    server: https://kubernetes.default.svc
    namespace: dev03-postman

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true

