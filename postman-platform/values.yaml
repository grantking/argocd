# Default values for helm-guestbook.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

configservice:
   replicaCount: 1

#grafana:
#   adminPassword: admin
#   datasources:
#      datasources.yaml:
#         apiVersion: 1
#         datasources:
#            - name: Prometheus - Postman
#              type: prometheus
#              access: proxy
#              url: http://postman-prometheus
#              isDefault: true
#
#prometheus:
#  fullnameOverride: postman-prometheus

consul:
  global:
    name: consul